#include <NovelRT/Graphics/GraphicsAdapter.hpp>

namespace NovelRT::Graphics
{
    /*
    template<typename TBackend>
    GraphicsAdapter<TBackend>::

    template<typename TBackend> uint32_t GraphicsAdapter<TBackend>::

    template<typename TBackend> const std::string& GraphicsAdapter<TBackend>::

    template<typename TBackend>
    std::shared_ptr<GraphicsProvider<TBackend>> GraphicsAdapter<TBackend>::

    template<typename TBackend> uint32_t GraphicsAdapter<TBackend>::

    template<typename TBackend>
    std::shared_ptr<GraphicsDevice<TBackend>> GraphicsAdapter<TBackend>::
    */
}
