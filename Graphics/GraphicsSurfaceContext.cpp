#include <NovelRT/Graphics/GraphicsSurfaceContext.hpp>

namespace NovelRT::Graphics
{
    /*template<typename TBackend>
    GraphicsSurfaceContext<TBackend>::

    template<typename TBackend>
    std::shared_ptr<IGraphicsSurface> GraphicsSurfaceContext<TBackend>::

    template<typename TBackend>
    std::shared_ptr<GraphicsProvider<TBackend>> GraphicsSurfaceContext<TBackend>::

    template<typename TBackend> void* GraphicsSurfaceContext<TBackend>::*/
}
